framework:
    messenger:
        failure_transport: failed

        transports:
            async: '%env(MESSENGER_TRANSPORT_DSN)%'
            failed: 'doctrine://default?queue_name=failed'

        routing:
            App\Message\WarmAudioMessage: async

when@test:
    framework:
        messenger:
            transports:
                async: 'in-memory://'
